<Window x:Class="KokoroGUI.Views.ModelDownloadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Kokoro TTS - Downloading Models"
        Icon="pack://application:,,,/KokoroGUI;component/app.ico"
        Height="240" Width="460"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource WindowBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="First-Launch Model Setup"
                   FontFamily="{StaticResource AppFont}" FontSize="16" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10" />

        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}"
                   FontFamily="{StaticResource AppFont}" FontSize="12"
                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,5" />

        <ProgressBar Grid.Row="2" Height="14" Value="{Binding ProgressPercent}" Maximum="100"
                     Style="{StaticResource CustomProgressBarStyle}" Margin="0,5,0,5" />

        <TextBlock Grid.Row="3" Text="{Binding DownloadDetails}"
                   FontFamily="{StaticResource MonoFont}" FontSize="11"
                   Foreground="{StaticResource TextMutedBrush}" Margin="0,2,0,10" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Cancel" Command="{Binding CancelCommand}"
                    Style="{StaticResource CancelButtonStyle}" Width="80" Height="30" />
        </StackPanel>
    </Grid>
</Window>
